# Security policy

This is a static site with no authentication, backend, or user-submitted form processing. Report a security issue through a private GitHub security advisory in the repository’s **Security** tab rather than a public issue.

Do not include real credentials or personal data in a proof of concept. The project supports the latest code on the default branch.
