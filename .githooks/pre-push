#!/bin/sh
set -eu

while read -r local_ref local_sha remote_ref remote_sha; do
  case "$local_sha" in
    0000000000000000000000000000000000000000) continue ;;
  esac
  node scripts/check-outgoing-privacy.mjs "$remote_sha" "$local_sha"
done
