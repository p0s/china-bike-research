# Published schemas

These JSON Schema files describe the main public JSON record types for editors and external tools, including product-image and curated-video records. The executable validator in `src/lib/data.mjs` adds cross-reference, uniqueness, date, range, local-file, image-rights, video-target, source, and price-history checks that JSON Schema alone does not cover.
