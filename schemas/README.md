# Published schemas

These JSON Schema files describe the main public JSON record types for editors and external tools. The executable validator in `src/lib/data.mjs` adds cross-reference, uniqueness, date, range, source, and price-history checks that JSON Schema alone does not cover.
